# Browser Sample

Open `index.html` in a browser to inspect the generated drawing preview.

Regenerate `sample-data.js` after changing the fixture or projection code:

```sh
npm run build
npm run example:browser:data
```
